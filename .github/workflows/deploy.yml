name: Deploy

on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [17.x]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: ${{matrix.node-version}}
      #SETUP KEY
      - run: set -eu
      - run: mkdir "$HOME/.ssh"
      - run: echo ${{secrets.SSH_PRIVATE_KEY}} > "$HOME/.shh/key"
      - run: chmod 600 "$HOME/.ssh/key"
      # BUILD
      # - run: npm ci 
      # - run: npm run build
      #DEPLOY
      - run: cd dist && rsync -e "ssh -i $HOME/.ssh/key -o StrictHostKeyChecking=no" --archive --compress --delete . vitossvx@185.224.132.106:/home/vitossvx/deploy/emgtestwork

